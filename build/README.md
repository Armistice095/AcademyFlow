# build/

Ressources supplémentaires pour `electron-builder` (ex: `background.png` pour l'installateur NSIS,
`installerSidebar.bmp`, etc.). Vide pour l'instant — à compléter en Phase 9 (Packaging Windows).

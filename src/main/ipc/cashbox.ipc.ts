// import { ipcMain } from 'electron'
// import { IPC_CHANNELS } from '@shared/ipc-channels'

/**
 * Handlers IPC du domaine Caisse (F-013 à F-021).
 * Squelette posé en Phase 2 — complété en Phase 7 (Module Caisse).
 */
export function registerCashboxIpcHandlers(): void {
  // TODO Phase 7 :
  // ipcMain.handle(IPC_CHANNELS.cashbox.createEntry, async (_event, data) => { ... })
  // ipcMain.handle(IPC_CHANNELS.cashbox.cancelTransaction, async (_event, transactionId, reason) => { ... })
  // ipcMain.handle(IPC_CHANNELS.cashbox.getJournal, async (_event, filters) => { ... })
  // ipcMain.handle(IPC_CHANNELS.cashbox.getStudentAccount, async (_event, studentId) => { ... })
  // ipcMain.handle(IPC_CHANNELS.cashbox.listArrears, async () => { ... })
  // ipcMain.handle(IPC_CHANNELS.cashbox.getReport, async (_event, from, to) => { ... })
}

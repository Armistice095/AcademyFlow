// import { ipcMain } from 'electron'
// import { IPC_CHANNELS } from '@shared/ipc-channels'

/**
 * Handlers IPC du domaine Personnel (F-022 à F-025).
 * Squelette posé en Phase 2 — complété en Phase 8 (Module Personnel).
 */
export function registerPersonnelIpcHandlers(): void {
  // TODO Phase 8 :
  // ipcMain.handle(IPC_CHANNELS.personnel.create, async (_event, data) => { ... })
  // ipcMain.handle(IPC_CHANNELS.personnel.update, async (_event, id, data) => { ... })
  // ipcMain.handle(IPC_CHANNELS.personnel.delete, async (_event, id) => { ... })
  // ipcMain.handle(IPC_CHANNELS.personnel.list, async () => { ... })
  // ipcMain.handle(IPC_CHANNELS.personnel.markSalaryPaid, async (_event, employeeId, month, year) => { ... })
  // ipcMain.handle(IPC_CHANNELS.personnel.getSalaryStatus, async (_event, month, year) => { ... })
}

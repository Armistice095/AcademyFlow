import { PlaceholderPage } from '@renderer/components/layout/PlaceholderPage'

export function SalaryTrackingPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Suivi des salaires"
      description="Grille mensuelle payé/non-payé par employé, marquage des paiements."
      phase="Phase 8"
    />
  )
}
